// Difference between extends and implements

// ✅ extends — Наслідування
// Використовується для успадкування (inheritance).
// Наслідує усі реалізації та методи базового класу (крім приватних).
// Можна перевизначати (override) методи, якщо хочеш змінити поведінку.
// Підходить для розширення поведінки.

void main() {
  Bird b = Bird();
  b.move(); // ✅ успадковано з Animal
  b.fly(); // ✅ метод Bird власний
}

class Animal {
  void move() {
    print('Рухається...');
  }
}

class Bird extends Animal {
  void fly() {
    print('Летить...');
  }
}

// ✅ implements — Реалізація інтерфейсу
// Не наслідує реалізацію — лише структуру (контракт).
// Потрібно реалізувати ВСІ методи вручну.
// Часто використовується для реалізації інтерфейсів чи абстракцій.

abstract class Flyer {
  void fly();
}

class Plane implements Flyer {
  @override
  void fly() {
    print('Летить як літак ✈️');
  }
}

// | Ознака                               | `extends`                                    | `implements`                                    |
// | ------------------------------------ | -------------------------------------------- | ----------------------------------------------- |
// | Наслідує реалізацію?                 | ✅ Так                                        | ❌ Ні                                            |
// | Може перевизначити методи?           | ✅ Так                                        | ✅ Так (але обов’язково реалізувати)             |
// | Чи обов’язково реалізовувати методи? | ❌ Ні                                         | ✅ Так                                           |
// | Коли використовувати?                | Коли хочеш **розширити** існуючий функціонал | Коли реалізовуєш **контракт** або **інтерфейс** |
// | Скільки класів можна наслідувати?    | ❗ Лише **один** (`extends`)                  | ✅ Скільки завгодно інтерфейсів (`implements`)   |

// 🧠 Аналогія:
// extends = "Я є більш конкретна версія тебе"
// implements = "Я обіцяю вести себе так само, як ти описав"

// Flutter-приклад:

abstract interface class AppState {
  void render();
}

class LoadingState implements AppState {
  @override
  void render() => print('Завантаження...');
}

class ErrorState implements AppState {
  @override
  void render() => print('Щось пішло не так...');
}
