// When use abstract and abstract interface

// ✅ Використовуй abstract class, коли:
// 🔧 Ти даєш часткову реалізацію;
// 🤝 Хочеш, щоб підкласи успадкували поведінку;
// 👀 Пишеш базовий клас, який буде розширюватися;
// 📦 Часто — для shared logic, default реалізацій, або шаблонних методів.

// 🧩 Приклади:

abstract class BasePage {
  void buildContent();

  void showFooter() {
    print('©2025 MyApp');
  }
}

class HomePage extends BasePage {
  @override
  void buildContent() {
    print('Відображаю контент головної сторінки');
  }
}

// Потім у Flutter цей клас можна розширити у конкретній сторінці.

// ✅ Використовуй abstract interface class, коли:
// ✍️ Хочеш створити контракт без реалізації;
// 🛡️ Хочеш заборонити наслідування, дозволяючи тільки реалізацію;
// 📄 Використовуєш інтерфейс як API;
// 🧪 Для тестування або Mock-класів;
// 💡 Це як "обіцянка" — цей клас **нічого не робить, але вказує, що має бути зроблено".

// 🧩 Приклади:
abstract interface class UserRepository {
  Future<String> getUserName();
}

class FirebaseUserRepository implements UserRepository {
  @override
  Future<String> getUserName() async => 'Ім’я з Firebase';
}

// А далі: FirebaseRepository implements Repository або MockRepository.

// | Ситуація                                        | Що обрати                  |
// | ----------------------------------------------- | -------------------------- |
// | Потрібна базова поведінка + структура           | `abstract class`           |
// | Потрібен чистий контракт                        | `abstract interface class` |
// | Хочеш мати методи з реалізацією (не абстрактні) | `abstract class`           |
// | Хочеш змусити реалізовувати все                 | `abstract interface class` |
// | Хочеш запобігти наслідуванню логіки             | `abstract interface class` |

// | Ознака                             | `abstract class`                 | `abstract interface class`      |
// | ---------------------------------- | -------------------------------- | ------------------------------- |
// | Тип призначення                    | Базовий клас із логікою          | Чистий контракт (інтерфейс)     |
// | Можна наслідувати (`extends`)      | ✅ Так                            | ❌ Ні                            |
// | Можна реалізувати (`implements`)   | ✅ Так                            | ✅ Так                           |
// | Має реалізацію методів             | ✅ Може                           | ❌ Не обов’язково, але можна     |
// | Обов'язкова реалізація в підкласах | ❌ Не всі методи                  | ✅ Усі абстрактні методи         |
// | Поширене використання              | Базові класи, логіка, UI шаблони | Репозиторії, API, state, mock-и |

// | Операція     | Що означає                      | Що отримуєш                          |
// | ------------ | ------------------------------- | ------------------------------------ |
// | `extends`    | Наслідуєш логіку базового класу | Методи, змінні, поведінка            |
// | `implements` | Реалізуєш контракт              | Лише сигнатури, реалізацію пишеш сам |

// | Що моделюєш                                             | Використовуй                              |
// | ------------------------------------------------------- | ----------------------------------------- |
// | Базовий UI з загальними методами (наприклад `BasePage`) | `abstract class` + `extends`              |
// | Контракт репозиторію (`UserRepository`)                 | `abstract interface class` + `implements` |
// | Модель стану (`AppState`)                               | `abstract interface class` + `implements` |
// | Mock-клас для тестів                                    | `abstract interface class` + `implements` |
